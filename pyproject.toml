[project]
name = "agenticrag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "strands-agents[anthropic]>=0.1.8",
    "strands-agents-tools>=0.1.6",
]
